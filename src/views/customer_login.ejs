<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Customer Login</title>
    <link rel="stylesheet" href="/css/site.css" />
  </head>
  <body>
    <header class="site-topbar">
      <div class="site-topbar__inner">
        <div class="site-brand">
          <div>
            <h1>Precioso</h1>
            <span>Facial Salon & Spa</span>
          </div>
        </div>
        <div class="site-links">
          <a href="/">Home</a>
          <a href="/services">Services</a>
          <a href="/branches">Locations</a>
          <a href="/contact">Contact</a>
          <% if (customerLoggedIn) { %>
            <a href="/bookings">My Bookings</a>
            <a href="/customer/logout">Logout</a>
          <% } else { %>
            <a href="/customer/login">Login</a>
            <a href="/customer/register">Register</a>
          <% } %>
          <a class="cta-button" href="/bookings/new">Book an Appointment</a>
        </div>
      </div>
    </header>

    <section class="site-hero">
      <h2>Customer Login</h2>
      <p>Home / Login</p>
    </section>

    <main class="site-main">
      <section class="form-card">
        <h3>Welcome Back</h3>
        <% if (error) { %>
          <p class="service-meta"><%= error %></p>
        <% } %>
        <form method="POST" action="/customer/login">
          <div class="form-grid">
            <div>
              <label>Email</label>
              <input type="email" name="email" required />
            </div>
            <div>
              <label>Password</label>
              <input type="password" name="password" required />
            </div>
          </div>
          <div style="margin-top: 18px;">
            <button type="submit">Login</button>
            <a class="cta-button" href="/customer/reset">Forgot password</a>
            <a class="cta-button" href="/customer/register">Create account</a>
          </div>
        </form>
      </section>
    </main>
  </body>
</html>




